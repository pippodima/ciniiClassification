# Server / CUDA requirements for the CiNii classification pipeline.
# Pinned to CUDA 11.8 + GTX 1080 Ti (11 GB VRAM, Compute Capability 6.1).
#
# Install in a fresh conda env or virtualenv:
#
#   conda create -n cinii python=3.11 -y && conda activate cinii
#   pip install -r pipeline/requirements-server.txt
#
# Recommended env vars for this GPU:
#   DEVICE=cuda
#   BATCH_SIZE=64          # safe for 11 GB VRAM with Qwen3-Embedding-0.6B
#   SAVE_EVERY_N_BATCHES=50

# ── PyTorch (CUDA 11.8) ───────────────────────────────────────────────────────
# GTX 1080 Ti is Pascal (CC 6.1) — fully supported on cu118, no Flash Attention.
--index-url https://download.pytorch.org/whl/cu118
torch>=2.2.0
torchvision>=0.17.0
torchaudio>=2.2.0

# ── Core ML / NLP ─────────────────────────────────────────────────────────────
--extra-index-url https://pypi.org/simple
sentence-transformers>=3.0.1
transformers>=4.40.0
tokenizers>=0.19.0
safetensors>=0.4.0
huggingface-hub>=0.23.0
datasets>=2.18.0

# ── Data / pipeline ───────────────────────────────────────────────────────────
pandas>=2.1.0
pyarrow>=14.0.0
numpy>=1.26.0
scikit-learn>=1.4.0
scipy>=1.12.0
tqdm>=4.66.0

# ── Language detection ────────────────────────────────────────────────────────
langdetect>=1.0.9

# ── Utilities ─────────────────────────────────────────────────────────────────
psutil>=5.9.0
requests>=2.31.0
