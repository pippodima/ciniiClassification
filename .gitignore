# ── Generated / large data files ─────────────────────────────────────────────
data/
clustering_output/
training_data/
mappings/
eda/

# models/ — only models/release/ is tracked; all other experiment folders ignored
models/v1/
models/v2/

# Classification output (generated on server)
classified/

# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
.Python
*.egg-info/
dist/
build/
*.egg

# ── Environments ─────────────────────────────────────────────────────────────
.venv/
venv/
env/
.env

# ── Jupyter ───────────────────────────────────────────────────────────────────
.ipynb_checkpoints/
*.ipynb

# ── macOS ─────────────────────────────────────────────────────────────────────
.DS_Store
*.DS_Store

# ── Model / checkpoint artifacts ─────────────────────────────────────────────
*.pt
*.pkl
*.npy
*.mmap
*.bin
*.safetensors

# ── Logs ─────────────────────────────────────────────────────────────────────
*.log

# ── Release model (must be last — last matching rule wins) ───────────────────
!models/release/
!models/release/**

# ── Local-only docs (notes/diary/thesis drafts — not for the public repo) ────
PROJECT_DIARY.md
CLAUDE.md
thesis/

# ── Local scratch at repo root (never publish) ───────────────────────────────
cinii_sample.parquet
extracted.csv
lcc_mapping_corrected_copy.csv

# ── Training-run data (keep small provenance, ignore the parquet blobs) ──────
training_runs/**/*.parquet
!training_runs/**/cluster_metadata.parquet
reports/thesis_figures/old/

# ── v3 experiment dir (its artefacts are published via models/release/) ──────
models/v3_300k/
